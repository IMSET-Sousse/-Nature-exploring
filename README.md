# Nature Exploring

# Run backend
cd backend
python -m pipenv install

# Start backend server

python -m pipenv run python app.py

# Frontend setup
cd frontend
npm install

# Start frontend server
npm start